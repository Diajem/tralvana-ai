# Task Tracker

Live register of all engineering tasks. Update status in the same PR that completes the work.

**Statuses:** `planned` · `in-progress` · `complete` · `blocked` · `cancelled`
**Priorities:** `critical` · `high` · `medium` · `low`

---

## Completed Tasks

| Task ID | Title | Status | Priority | Commit | Notes |
|---------|-------|--------|----------|--------|-------|
| T-001 | Bootstrap project foundation | `complete` | critical | Sprint 0 | Next.js 15 + FastAPI + AI directory structure |
| T-001B | Complete Sprint 0 scaffold | `complete` | critical | Sprint 0 | AI agent layer, orchestrator, memory schema, architecture docs |
| T-002 | Initial commit and remote push | `complete` | high | Sprint 0 | Repo live at github.com/Diajem/tralvana-ai |
| T-003 | Create Architecture Authority | `complete` | high | Sprint 0 | ARCHITECTURE.md, ENGINEERING_PRINCIPLES.md, CODING_STANDARDS.md, ROADMAP.md |
| T-004 | Traveller DNA + Travel Intelligence Layer | `complete` | critical | Sprint 1 | `ai/intelligence/` — KG (199 nodes, 205 edges), ontology, reasoning, DNA inference (12 types), memory service |
| T-005 | Traveller Profile API | `complete` | high | Sprint 1 | `POST /traveller/profile`, `GET /traveller/profile/{id}` — in-memory store; onboarding frontend |
| T-006 | Conversation Engine | `complete` | high | Sprint 1 | `ai/concierge/` — intent classifier, decision engine, session store, TravelConcierge facade; `POST /conversation/message` |
| T-007 | Goal Planning Engine | `complete` | high | Sprint 1 | `ai/goals/` + `services/api/app/domains/goals/` — GoalReasoner, CRUD, readiness scoring; `/goals/new` + `/goals/[id]` |
| T-008 | Trip Planning Engine | `complete` | high | `889ab4c` | `ai/planning/` + `services/api/app/domains/trips/` — itinerary/budget/risk; trip auto-trigger from conversation; `/trips/new` + `/trips/[id]` |
| T-009 | End-to-End TravelOS Integration Demo | `complete` | high | `8a8ab1e` | `services/api/app/demo/` — 7-stage pipeline; `POST /demo/japan-football-food`; frontend `/demo` |
| T-010 | Repository Audit and Architecture Review | `complete` | medium | `1d10953` | Audit doc, tech debt register, architecture recommendations, stray file cleanup |
| T-011 | Platform Layer | `complete` | high | `aa5934a` | `travelos/` — SDK, DI container, service registry, config, structured logging, event bus, shared types (`Result`, `Identifier`, `Timestamp`, `Pagination`); ADR-006 |
| T-012 | Testing Framework | `complete` | critical | `aa5934a` | pytest suite — 30 backend API tests + 62 AI layer tests (92 total, all passing); ADR-007; resolves TD-007 |
| T-013 | GitHub Actions CI/CD | `complete` | high | `2a17d6c` | `.github/workflows/ci.yml` — `pytest` (required), Ruff + frontend lint/build (advisory, see TD-016/TD-017); ADR-008 |
| T-014 | Repository Stabilisation & Engineering Refactor | `complete` | high | `07f63eb` | Closed TD-001 (legacy conversation layer removed), TD-002/TD-003 (dead `ai/orchestration/` removed), TD-004 (found to be a false positive — the 5 specialist agents are live, not dead), TD-005 (duplicate `AgentContext`/`AgentResult` resolved), TD-016 (ESLint config fixed), TD-017 (Ruff violations 72 → 0). No API or behaviour changes; 92/92 tests pass throughout. ADR-009 |
| T-015 | Flight Intelligence Engine | `complete` | high | `85664fe` | First Discovery Layer module — `services/api/app/domains/flights/` + `ai/discovery/flights/` (scorer/reasoner/risk assessor/orchestrator); `POST /flights/recommend`, `GET /flights/{id}`, `GET /trips/{id}/flights`; new `Intent.FLIGHT_SEARCH` conversation routing; `/flights/recommend` + `/flights/[id]` frontend; 49 new tests (141 total, all passing); ADR-010 |
| T-016 | Discovery Layer Pattern and Accommodation Intelligence | `complete` | high | `2a7112d` | `docs/DISCOVERY_LAYER_PATTERN.md` (ADR-011) formalises the 7-stage pipeline. Second Discovery module — `services/api/app/domains/accommodation/` + `ai/discovery/accommodation/` (provider/normalizer/scorer/reasoner/risk assessor/orchestrator, first module with a genuine Provider/Normalizer split); `POST /accommodation/recommend`, `GET /accommodation/{id}`, `GET /trips/{id}/accommodation`; new `Intent.ACCOMMODATION_SEARCH` conversation routing; `/accommodation/recommend` + `/accommodation/[id]` frontend; found and fixed a labelling-algorithm pigeonhole bug and a shared entity-extractor bug ("to stay" misread as a destination); 70 new tests (211 total, all passing); ADR-012 |
| T-017 | Destination Intelligence Engine | `complete` | medium | `0f01b86` | Third Discovery module — `services/api/app/domains/destinations/` + `ai/discovery/destinations/` (provider/normalizer/scorer/reasoner/risk assessor/orchestrator); 10-city mock catalogue (60 entries, all 12 `DestinationType` values); dual-mode search (city given → places within it, omitted → compare cities); `POST /destinations/recommend`, `GET /destinations/{id}`, `GET /trips/{id}/destinations`; new `Intent.DESTINATION_DISCOVERY` conversation routing (always-ready, no destination required); `/destinations/recommend` + `/destinations/[id]` frontend; found and fixed a persona-relevance bug (irrelevant categories like BEST_FOR_FOOTBALL forced onto football-irrelevant cities) and an off-season boundary bug in the risk assessor; 78 new tests (289 total, all passing); ADR-013 |
| T-018 | Budget Intelligence Engine | `complete` | medium | `2a403b3` | Fourth Discovery module — `services/api/app/domains/budget/` + `ai/discovery/budget/` (provider/normalizer/scorer/reasoner/risk assessor/orchestrator); candidate set is the 5 budget styles themselves (backpacker/budget/balanced/comfort/luxury) rather than provider inventory; regional rate tables reuse `BudgetReasoner`'s existing values for cross-component consistency; cap-fit against a linked Goal's `budget.max_usd` is the dominant (0.40) scoring dimension; `POST /budget/recommend`, `GET /budget/{id}`, `GET /trips/{id}/budget`; new `Intent.BUDGET_ANALYSIS` conversation routing (always-ready, distinct from the pre-existing `BUDGET_ADVICE` intent); `/budget/recommend` + `/budget/[id]` frontend; 78 new tests (367 total, all passing); ADR-014 |
| T-019 | Visa Intelligence Engine | `complete` | medium | `283bcb6` | Fifth Discovery module — `services/api/app/domains/visa/` + `ai/discovery/visa/` (provider/normalizer/scorer/reasoner/risk assessor/orchestrator); single-assessment shape, not a ranked list — the module's deliberate structural departure from the pattern's labelling algorithm; 10 mock nationalities × 8 destinations via a 2-tier formula plus CTA/ECOWAS/same-country overrides; Scorer computes assessment confidence rather than a subjective preference match; `POST /visa/check`, `GET /visa/{id}`, `GET /trips/{id}/visa`; new `Intent.VISA_CHECK` conversation routing plus a nationality+destination fallback inference rule in `IntentClassifier`; `/visa/check` + `/visa/[id]` frontend; explicit "not legal advice" disclaimer throughout; 100 new tests (467 total, all passing); ADR-015 |
| T-020 | Weather & Safety Intelligence Engine | `complete` | medium | `c0be1c0` | Sixth and final Discovery module — `services/api/app/domains/weather/` + `ai/discovery/weather/` (provider/normalizer/scorer/reasoner/risk assessor/orchestrator); combines Visa's single-assessment shape with Destination's dual-mode requests (month given → assess it; omitted → find and assess the best month); 10 destinations authored as 2-4 season blocks expanded to all 12 months; Risk Assessor returns a structured object (risks + 3 categorical risk levels + safety summary), the module's one deviation from the flat-list Risk Assessor shape used elsewhere; deliberately independent of Destination/Budget Intelligence per an explicit task constraint; `POST /weather/analyse`, `GET /weather/{id}`, `GET /trips/{id}/weather`; new `Intent.WEATHER_ANALYSIS` conversation routing; found and fixed two pre-existing entity-extraction bugs in shared `IntentClassifier` code ("visit" swallowed by the "to " marker, "in " matching inside "rain"); `/weather/analyse` + `/weather/[id]` frontend; 107 new tests (574 total, all passing); ADR-016. Completes the Discovery Layer as scoped in this tracker |
| T-021 | Trip Brain Architecture | `complete` | medium | (pending commit) | Architecture only — no code, no new APIs, no new modules, per explicit task constraint. Five documents: `docs/TRIP_BRAIN_ARCHITECTURE.md` (responsibilities, orchestration/request/decision lifecycles, memory, session management, failure handling, confidence propagation, explainability strategy, sequence/component diagrams), `docs/EPIC3_ARCHITECTURE.md` (Epic 3 framing, Knowledge Integration scope vs. Phase 5's separate vector-store/RAG work), `docs/ORCHESTRATION_PATTERN.md` (Planner/Coordinator/Discovery Modules/Knowledge Sources/Response Composer/Memory/Feedback Loop, reference-implementation table), `docs/KNOWLEDGE_SOURCE_STRATEGY.md` (precise Knowledge Source vs. Provider vs. API vs. Repository vs. Knowledge Graph vs. Internal Memory taxonomy), `docs/ADR/ADR-017-trip-brain.md`. Key finding: `PLAN_TRIP` currently routes through Sprint-1 placeholder agents (`ai/manager/TravelManager` + `ai/registry/AgentRegistry`) instead of the six real Discovery modules — Trip Brain is architected to supersede that *active call site* (T-022) while the legacy code stays in place as a rollback path until a distinct later cleanup task (T-023). Zero code/API/module changes — `git status` shows only new files under `docs/` |
| T-022 | Trip Brain Implementation | `complete` | high | (pending commit) | Builds `docs/TRIP_BRAIN_ARCHITECTURE.md`/ADR-017 exactly as documented. New `ai/trip_brain/` package — `context.py` (`ContextBuilder`/`TripBrainContext`, request-scoped, no new memory store), `module_selection.py` (`ModuleSelector`, static weighted rule table — core weight 1.0 for destination/weather always and flight/accommodation once dates are known, supporting weight 0.7 for budget/visa when conditionally triggered, all six forced to core on a full destination+dates+party-size trip shape), `discovery_adapters.py` (Trip Brain's only call sites into the six Discovery modules — each calls the same public `*_from_conversation()` service method `ConversationEngine` already calls for narrow intents, never a Provider or Repository), `confidence.py` (`overall_confidence = weighted_average(per_module_confidence, weight=module_relevance) × completion_penalty`, exactly the doc's formula), `conflicts.py` (the one documented budget-tier-vs-accommodation-pick presentation conflict, surfaced as an assumption, never a score mutation), `synthesis.py` (one preamble sentence naming what was checked), `coordinator.py` (`TripBrain.plan()` — runs selected modules concurrently via `asyncio.to_thread`, isolates each module's failure independently, never aborts the others). `ConversationEngine` now routes `Intent.PLAN_TRIP` through `trip_brain.plan()` instead of `TravelManager`; `ResponseComposer.compose()` gained an additive `synthesis_note` parameter and its "no results" fallback now also fires when every present result is `FAILED` (the Total Failure Floor), not only when the results list is empty — both changes are backward-compatible no-ops for every pre-existing caller. `ai/manager/`, `ai/registry/`, and the five placeholder agent files are **not touched** — `git status` confirms zero diff under those paths; `MODIFY_TRIP`/`DESTINATION_QUESTION`/`TRAVEL_ADVICE`/`BUDGET_ADVICE` continue through `TravelManager` unchanged, exactly as ADR-017 scopes it (only `PLAN_TRIP` is repointed). Found and fixed a pre-existing shared-code bug in `IntentClassifier._extract_entities` (same class of bug T-020 already fixed twice in this method): "I want to travel to Tokyo" / "I need to fly to Barcelona" / "I want to plan a trip to Tokyo" all misread the auxiliary verb ("travel"/"fly"/"plan") as the destination because the "to " marker search stopped at the first occurrence, colliding with "want to"/"need to" infinitive constructions; fixed by having the "to " marker keep scanning past known infinitive-verb candidates for the real prepositional "to" — needed for Trip Brain's own correctness since these are the most natural PLAN_TRIP phrasings. 75 new tests (649 total, all passing): module selection, context building (including unknown goal/trip id tolerance), confidence aggregation, conflict detection, per-module adapter success/failure isolation, coordinator-level parallel execution/partial failure/total failure (including a timing-based concurrency assertion), `PLAN_TRIP` routing through Trip Brain, narrow-intent regression (all six narrow intents plus `MODIFY_TRIP`'s continued legacy path unchanged), and the `ResponseComposer` additive extension. Ruff clean |
| T-023 | Legacy Orchestration Retirement and Repository Cleanup | `complete` | medium | Investigation task, not a deletion task in practice — see ADR-018. A full repository-wide import/runtime-reference trace of `ai/manager/`, `ai/registry/`, and the five placeholder agents found the ADR-017/T-022-era premise wrong: `ConversationEngine.process()` still dispatches `MODIFY_TRIP`, `DESTINATION_QUESTION`, `TRAVEL_ADVICE`, and `BUDGET_ADVICE` through `travel_manager.execute()` (`ai/concierge/conversation_engine.py:212`) — Trip Brain only ever superseded `PLAN_TRIP`, never the whole legacy stack. Deleting `ai/manager/`, `ai/registry/`, or the five agents as originally scoped would have broken those four live, tested intents, violating this task's own "preserve public API behaviour" constraint — so **zero production code, tests, or public API shapes were changed**. Confirmed `recommended_agents` (on both the conversation response and each Discovery module's schema) is separate, intentional, tested public-contract surface, not legacy orchestration. One dead method found and left alone as out-of-scope (`TravelManager.list_agents()`, zero callers, a method not a file). What changed: corrected the inaccurate "uncalled/dormant" language in `ai/concierge/conversation_engine.py`'s routing comment, `docs/TRIP_BRAIN_ARCHITECTURE.md`, and `docs/ARCHITECTURE.md` (which had never been updated for Trip Brain at all since T-022); added `docs/ADR/ADR-018-legacy-orchestration-retirement.md`; opened `docs/TECHNICAL_DEBT_REGISTER.md` TD-018 and backlog task T-032 (migrate the four remaining intents off `TravelManager` — the actual prerequisite for eventual deletion). All 649 tests pass unchanged; Ruff clean; frontend typecheck/build unaffected (no frontend files touched) |
| T-024 | Explainability Engine | `complete` | medium | New `ai/explainability/` package — `explainability_engine.py` (`ExplainabilityEngine.explain()`/`answer_question()`), `explanation_builder.py` (drivers, alternatives considered, missing information, what-would-change, source attribution, summary), `confidence_explainer.py` (bands + reduction reasons), `tradeoff_analyser.py` (five deterministic trade-off patterns — price vs. journey length, value vs. accommodation cost, comfort vs. budget, visa vs. destination, weather vs. price; "shorter trip vs. more activities" deliberately not implemented — no supporting field exists in current Discovery output), `assumption_explainer.py`/`risk_explainer.py` (verbatim, deduplicated collection). Reuses existing reasoning only — no new scores, no new travel logic. `ai/trip_brain/coordinator.py` calls it once per `plan()`, right after conflict resolution and confidence aggregation, attaching the result to a new `UnifiedRecommendation.explanation` field (plus `conflicts` — previously computed and discarded — and `destination`). `ai/trip_brain/discovery_adapters.py` gained one additive `data` key, `alternative_option` (flight/accommodation/budget), surfacing each module's own already-computed cheapest-labelled option for trade-off comparison — no score or `top_option` selection changes. New `Intent.EXPLAIN_RECOMMENDATION` (`ai/concierge/intent_classifier.py`) matches follow-ups ("why did you recommend this?", "why not the cheaper option?", "what assumptions did you make?", "how confident are you?", "what would change your answer?"), checked before `PLAN_TRIP`/`TRAVEL_ADVICE`/`BUDGET_ADVICE` to avoid their broader keywords swallowing it. `ConversationSession` gained `last_recommendation`, populated only by `PLAN_TRIP`'s Trip Brain path; `EXPLAIN_RECOMMENDATION` reads it and calls `answer_question()` directly rather than `ResponseComposer.compose()` — avoids re-rendering every module's full section on every follow-up (see ADR-019's Alternatives Considered). New `POST /explain` (`services/api/app/routers/explain.py`) — three input modes (`module_results`, `conversation_id`, `trip_id`), `404` when none resolves, `400` on an invalid module-result status. New `apps/web/src/app/explain/page.tsx`; one additive link from `apps/web/src/app/trips/[id]/page.tsx`. Docs: `docs/EXPLAINABILITY_ENGINE.md`, `docs/API_EXPLAINABILITY.md`, `docs/ADR/ADR-019-explainability-engine.md`; updated `docs/TRIP_BRAIN_ARCHITECTURE.md` and `docs/ARCHITECTURE.md`. 74 new tests (723 total, all passing): explanation generation, confidence bands, trade-off detection (including negative cases), assumption/risk preservation and deduplication, source attribution, partial module failure visibility (raw exception text never leaked to the traveller), conflicting results, follow-up intent routing (including non-collision with `FLIGHT_SEARCH`/`TRAVEL_ADVICE`), deterministic output, and Trip Brain integration. Ruff clean; frontend typecheck and build clean. T-032 (`ai/manager`/`ai/registry` retirement) untouched, as instructed |
| T-025 | Intelligence Gateway | `complete` | medium | Provider-access infrastructure only — no live external API integrated. New `travelos/intelligence_gateway/` (deviates from the task's suggested `platform/intelligence_gateway/` path — the repo's platform layer is `travelos/`, not `platform/`; see ADR-020) — `provider_contract.py` (`Provider` ABC, `ProviderRequest`), `provider_result.py`, `provider_status.py` (`ProviderEnvironment`/`ProviderStatus`/`Capability` enums, 9 capabilities as specified), `provider_registry.py`, `provider_selector.py` (deterministic: environment, health, request support, priority, fallback order — no ML), `retry_policy.py` (stdlib only, exponential backoff, validation/auth/misconfiguration never retried), `cache_policy.py` (in-memory, capability-specific TTL defaults, stale-data indication for graceful degradation), `rate_limit_policy.py` (in-memory, per-provider), `failover_policy.py` (try-next-on-failure, preserves original failure, names the answering provider), `secret_reference.py` (env-var-backed, `resolve()` vs. `describe()`/`repr()` never exposes the value), `exceptions.py`, `gateway.py` (`IntelligenceGateway.execute()` ties it together), `discovery_adapters.py` (two-layer integration — gateway-contract wrappers around the existing `Mock{Flight,Accommodation,Weather}Provider` classes, plus drop-in `Gateway{Flight,Accommodation,Weather}Provider` adapters matching each Discovery module's existing provider constructor argument exactly). All three minimum-required providers (Flight, Accommodation, Weather) actually wired into their live singletons (`ai/discovery/{flights,accommodation,weather}/*_intelligence.py`, one line changed each), not merely made accessible — proven behaviour-preserving via byte-identical-output tests and the full pre-existing suite passing unchanged. Found and fixed a real bug during testing: rate-limited providers were returning a `RATE_LIMITED` result instead of raising, which `run_with_failover` treated as success instead of failing over — fixed by raising `ProviderRateLimitedError` (see ADR-020). `ConfigurationManager` gained `provider_environment`/`cache_enabled`/`retry_enabled`/`default_provider_priority`/`provider_override_for()`, all env-var-driven with dev-safe defaults. New `GET /internal/providers/status` (`services/api/app/routers/internal.py`) — safe metadata only, verified no secret/credential/token string ever appears in the response. New `travelos/tests/` test root (added to `pytest.ini`) — a first step toward T-012A, not a scope expansion of it. Destinations/Budget/Visa deferred (same pattern, not wired to keep the diff reviewable); Maps/Currency/Events deferred (no Discovery module exists yet to wrap). Docs: `docs/INTELLIGENCE_GATEWAY.md`, `docs/PROVIDER_CONTRACT.md`, `docs/PROVIDER_SELECTION.md`, `docs/CACHING_AND_FAILOVER.md`, `docs/SECRET_MANAGEMENT.md`, `docs/ADR/ADR-020-intelligence-gateway.md`; updated `docs/ARCHITECTURE.md`, `docs/TRIP_BRAIN_ARCHITECTURE.md`, `docs/TECHNICAL_DEBT_REGISTER.md`, `README.md`, `.env.example`. Displaced the planned "PostgreSQL persistence" backlog item from T-025 to T-034 (next unused id). 110 new tests (833 total, all passing): provider registration, capability lookup, selection, unsupported requests, cache hit/miss/TTL expiry, retry behaviour, non-retryable errors, failover, total failure, rate-limited failover, missing secret detection, secret redaction, deterministic mock-provider behaviour (byte-identical to calling the mock provider directly), diagnostics output, no public API regression. Ruff clean; frontend typecheck/build unaffected (no frontend files touched — this task is backend/platform-only) |
| T-026 | Live Provider Framework | `complete` | medium | Foundation only — no real external API connected. New `travelos/live_providers/` — `base_live_provider.py` (`BaseLiveProvider(Provider)`, a template-method `execute()`: validate → authenticate → build_request → send_request → check HTTP status → parse_response → `ProviderResult`; a concrete provider implements only `build_request()`/`parse_response()`), `transport.py` (`Transport` ABC + `FakeTransport`, the only concrete transport — no real HTTP client, no live network call anywhere), `auth/` (`ApiKeyAuthStrategy`, `BearerTokenAuthStrategy`, `OAuth2ClientCredentialsAuthStrategy` — interfaces only, the OAuth2 strategy's `headers()` raises unless a token is injected via a test-only `set_cached_token()`, no real token exchange implemented), `errors/` (re-exports `travelos.intelligence_gateway.exceptions` rather than a second hierarchy; added `ProviderResponseError` and aliased `ProviderRateLimitError`/`ProviderConfigurationError` to T-025's `ProviderRateLimitedError`/`ProviderMisconfiguredError`), `health/` (`ProviderHealthResult`), `metrics/` (`ProviderMetricsTracker` — request/success/failure/rate-limit counts, latency, optional cost never invented, only ever adapter-reported), `tracing/` (`RequestTrace`, logged via the existing `TravelLogger`, safe fields only), `templates/example_flight_provider.py` (`ExampleFlightProvider`, a clearly-labelled non-production template proving the full lifecycle against `FakeTransport`, including a `.invalid`-TLD request URL as a belt-and-braces guarantee). Key finding: the Intelligence Gateway (T-025) needed **zero code changes** — `gateway.py`/`provider_registry.py`/`provider_selector.py` are untouched; `BaseLiveProvider` is just another `Provider`, so registration/selection/retry/failover all work unmodified, proven by a dedicated test file registering a mock stub and the live template into one registry and exercising environment-based selection and live-to-mock failover. `ConfigurationManager` gained `provider_http_timeout_seconds`/`provider_retry_max_attempts`/`provider_healthcheck_enabled`, plus `TRALVANA_PROVIDER_ENVIRONMENT` as a new preferred name for T-025's `provider_environment` (falls back to the original `PROVIDER_ENVIRONMENT`, nothing already configured breaks). `GET /internal/providers/status` extended (additive) with `provider_type` (MOCK/LIVE), `authentication_configured`, `last_check_time`, `request_count`, `failure_count` — the one T-025 test asserting an exact field set was updated to the new superset. Displaced the planned "Redis session store" backlog item from T-026 to T-035 (next unused id). Docs: `docs/LIVE_PROVIDER_FRAMEWORK.md`, `docs/LIVE_PROVIDER_ADAPTER_GUIDE.md`, `docs/PROVIDER_AUTHENTICATION.md`, `docs/PROVIDER_ERROR_MODEL.md`, `docs/PROVIDER_OBSERVABILITY.md`, `docs/PRODUCTION_READINESS.md`, `docs/ADR/ADR-021-live-provider-framework.md`; updated `docs/INTELLIGENCE_GATEWAY.md`, `docs/ARCHITECTURE.md`. 80 new tests (913 total, all passing): base provider execution lifecycle, all three auth strategies (configuration, secret absence, secret redaction), request/response mapping, all seven standard error types via HTTP status mapping, malformed response, health checks, request tracing, usage metrics (including cost-reporting only-when-explicit), fake transport, mock/live registration, environment-based selection, live-to-mock failover, no public API regression. Ruff clean; frontend typecheck/build unaffected (backend/platform-only). Deferred: a real HTTP transport implementation, a real OAuth2 token exchange, retrofitting metrics onto T-025's mock providers, and any actual vendor adapter (Duffel, Amadeus, ...) — none required by this task's scope |
| T-027 | First Live Flight Provider Integration | `complete` | medium | First real vendor adapter on the Live Provider Framework (T-026) — no real network call made anywhere, not registered by default. Evaluated Duffel, Amadeus for Developers, Kiwi.com (Tequila), and Skyscanner against sandbox availability, docs quality, auth complexity, coverage, commercial suitability, scalability, and licensing (`docs/FIRST_LIVE_PROVIDER.md`); **recommended and built Duffel**, not Amadeus (explicitly not assumed available per this task's constraint) — the deciding factor was authentication: Duffel's static bearer token maps directly onto the already-fully-implemented `BearerTokenAuthStrategy`, while Amadeus (and every other viable alternative) requires an OAuth2 client-credentials exchange `OAuth2ClientCredentialsAuthStrategy` doesn't implement (TD-022). New `travelos/live_providers/adapters/duffel_flight_provider.py` — `DuffelFlightProvider(BaseLiveProvider)` mapping Duffel's `POST /air/offer_requests` to and from the exact internal flight-option dict shape `ai/discovery/flights/flight_scorer.py`/`flight_reasoner.py`/`flight_risk_assessor.py` already read from `MockFlightProvider.search()` — stricter than `ExampleFlightProvider`'s template, proven by a test that runs real scorer/reasoner/risk-assessor code against Duffel-shaped output. `map_error()` overridden to reclassify Duffel's structured 422 validation-error body (via a `send_request()` override that stashes the last response, since `map_error()` only receives the exception) into a non-retryable `ProviderValidationError` — verified against `RetryPolicy` directly, not just by exception name. `register_duffel_flight_provider()` added but **not** called at import time (unlike T-025's mock providers) — no default `Transport` exists to register against (TD-021 still open), and defaulting to `FakeTransport` would look production-configured while silently serving canned data under a real environment flag. Zero changes to `travelos/intelligence_gateway/`, `ai/discovery/flights/`, `ai/trip_brain/`, or `services/api/app/domains/flights/` — the Intelligence Gateway needed no changes for a second live provider any more than T-026 needed changes for the first. Docs: `docs/FIRST_LIVE_PROVIDER.md`, `docs/FLIGHT_PROVIDER_INTEGRATION.md`, `docs/ADR/ADR-022-first-live-provider.md`; updated `docs/PRODUCTION_READINESS.md`, `.env.example`, `README.md`. 35 new tests (948 total, all passing): request mapping (one-way/round-trip/auth headers), response mapping (direct and one-stop offers, multi-offer, source metadata), malformed-response handling, all standard HTTP-status error types, Duffel-specific 422 body reclassification and its retry-policy effect, health checks (including secret-redaction), environment defaulting/rejection, unsupported-operation validation, registration, environment-based selection, live-to-mock failover, and end-to-end compatibility through the unmodified `GatewayFlightProvider.search()` entry point. Ruff clean; frontend unaffected (backend/platform-only). Deferred (see `docs/FIRST_LIVE_PROVIDER.md`'s "What Remains Before Real Production Use"): a real HTTP transport, a real Duffel sandbox token exercised against Duffel's actual API, application-startup wiring, adult-only passenger count (pre-existing `GatewayFlightProvider.search()` limitation, out of scope per "no Discovery Layer redesign"), and Amadeus's OAuth2 exchange (TD-022, unchanged) |
| T-037 | Real HTTP Transport and First Live Provider Integration | `complete` | high | Closes TD-021 — the first real (non-fake) `Transport`, and the first genuine live call this repository has ever made. New `travelos/live_providers/httpx_transport.py` — `HttpxTransport(Transport)`, a thin `httpx.Client`-backed translation of `TransportRequest`/`TransportResponse` (`httpx==0.28.1`, already a pinned dependency — no new one added); carries no provider-specific logic and no `SecretReference` of its own, only ever forwarding whatever headers `execute()` already assembled upstream. Unit-tested (10 new tests) against `httpx.MockTransport` — no real socket opened in the automated suite, matching T-027's "never require internet access during CI" rule. With a real user-supplied `duffel_test_...` sandbox token (configured directly in the untracked, gitignored `.env` — never read, printed, logged, or committed by any step of this task), executed one live `IntelligenceGateway.execute()` call through the full unmodified stack (`ProviderRegistry` → `IntelligenceGateway` → `DuffelFlightProvider` → `HttpxTransport` → `https://api.duffel.com`, SANDBOX environment, still isolated behind the Gateway, no public API changed) via a manual, uncommitted verification script — **not** a pytest test. First attempt surfaced a real mapping bug `FakeTransport`-only testing couldn't have caught: Duffel returns some offer durations as `P1DT5H15M` (a day component, for connections spanning past 24 hours) rather than always `PT#H#M`; `_ISO8601_DURATION_RE`/`_parse_iso8601_duration` in `duffel_flight_provider.py` fixed to accept an optional day component, with a new regression test built from the exact real-world shape (not a hypothetical). After the fix, a second live call against Duffel's real SANDBOX API returned HTTP 201 with 235 real offers, every one of which mapped cleanly through `_map_offer()` into the existing internal flight-option shape (`gateway_provider_status: AVAILABLE`, `request_success: True`, `offer_count: 235`) — confirmed the adapter's contract holds against real vendor data, not only hand-built fixtures. Diagnosis of the original failure used a values-never-printed, key-names-and-types-only structural introspection technique (bounded-depth recursive type walk) to avoid exposing any live response content while still identifying the exact failing field. No credential value, request header, or full response payload was ever printed, logged, or committed at any point in this task. Zero changes to `travelos/intelligence_gateway/`, `ai/discovery/flights/`, `ai/trip_brain/`, or `services/api/app/domains/flights/`; `register_duffel_flight_provider()` still not auto-registered at import — this task proved the transport works, it did not wire it into application startup. Docs: updated `docs/FIRST_LIVE_PROVIDER.md` ("What Remains" — real transport and real sandbox verification items now closed), `docs/FLIGHT_PROVIDER_INTEGRATION.md`, `docs/PRODUCTION_READINESS.md`, `docs/ADR/ADR-023-real-http-transport-and-live-verification.md` (new), `README.md`. 11 new automated tests (959 total, all passing): 10 for `HttpxTransport` (request/response translation, query params, JSON/text body fallback, status/header passthrough, client lifecycle, no-credential-echo) plus 1 regression test for the day-component ISO 8601 duration bug found live. Ruff clean. Not committed — pending review, per explicit instruction |
| T-038 | Live Flight Search Product Integration | `complete` | high | Turns T-027/T-037's verified Duffel connection into a user-facing capability — `POST /flights/recommend` can now return real, ranked, explained Duffel sandbox offers, switchable via `TRALVANA_FLIGHT_PROVIDER_MODE` (`MOCK` default / `LIVE_SANDBOX`), with `MOCK` behaviour byte-identical to before this task. Search and recommendation only — no booking, payment, ticketing, order creation, seat selection, or baggage purchase anywhere. **Configuration**: `ConfigurationManager` gained `flight_provider_mode`/`flight_mock_fallback_enabled`; never inferred from `DUFFEL_API_TOKEN`'s mere presence. **Gateway**: `IntelligenceGateway._environment_for(capability)` (`travelos/intelligence_gateway/gateway.py`) — FLIGHTS resolves its environment from the new switch, every other capability (Accommodation, Weather) unchanged, resolving from the pre-existing `provider_environment` — solves the one hard architectural problem (a single gateway-wide environment scalar can't serve two different intents for two different capabilities) without a second Gateway instance or a second DI system. **Startup wiring**: new `travelos/live_providers/flight_provider_bootstrap.py`'s `configure_flight_provider()`, called once from `services/api/app/main.py` (the actual composition root, never a route handler, never `ai/discovery/flights/`) — a true no-op in `MOCK` mode; in `LIVE_SANDBOX` mode without `DUFFEL_API_TOKEN`, raises `FlightProviderMisconfiguredError` and **fails the application process at startup**, not per-request. **Validation**: new `ai/discovery/flights/live_search_validator.py`'s `validate_live_flight_search()` (IATA codes, date sanity, 1–9 adults, cabin class) — called from `FlightIntelligenceService.recommend()` only when `flight_provider_mode == LIVE_SANDBOX`, deliberately exempting `MOCK` mode's existing city-name-based requests (e.g. `origin="London"`) from ever breaking. **Public API**: `FlightRecommendationResponse` gained five additive fields (`data_source`, `retrieved_at`, `provider_status`, `results_count`, `request_id`); each `FlightOptionResponse` also gained `data_source` so a single flight fetched later via `GET /flights/{id}` still carries its own sandbox/mock label. `FlightOption.provider_offer_id` preserves Duffel's own offer id internally for future booking work but is deliberately excluded from `to_dict()`'s public shape. **Response processing**: `GatewayFlightProvider` (`travelos/intelligence_gateway/discovery_adapters.py`) gained `last_result`/`used_mock_fallback`, duck-typed by `FlightIntelligence._source_metadata()` to build the fields above without changing `search()`'s existing `list[dict]` contract. **Failure/fallback**: new `LiveFlightSearchUnavailableError` — a `LIVE_SANDBOX` failure returns `503` by default (never a silent empty result); `TRALVANA_FLIGHT_MOCK_FALLBACK_ENABLED=true` switches to `MockFlightProvider` directly, labelled `data_source: "MOCK_FALLBACK"` — a fully separate code path, so mock and live offers are never blended in one result set. `DuffelFlightProvider.parse_response()` (`travelos/live_providers/adapters/duffel_flight_provider.py`) changed to tolerate **partial mapping failure** — one malformed offer in a batch of 200+ no longer discards the whole response (T-027's original all-or-nothing behaviour); only raises if every offer in a non-empty batch fails, and now records `raw_offer_count`/`mapped_offer_count` in `source_metadata`. **Frontend**: `apps/web/src/app/flights/recommend/page.tsx` gained a source badge, a sandbox/fallback banner ("Sandbox flight data — not available for purchase."), and baggage/refundability/flexibility display on every card; `apps/web/src/app/flights/[id]/page.tsx` gained the same banner driven by the per-option `data_source`. Nothing anywhere presents a result as bookable. **Verification**: new `scripts/verify_duffel_live_sandbox.py` — reads `.env`, runs one real search through the *full* product architecture (not just the raw Gateway, as T-037's script did), reports `http_status_code`/`provider_status`/`data_source`/`raw_offer_count`/`normalised_offer_count`/`ranked_offer_count`/`request_id`, never the token. Run live during this task: HTTP 201, 235 raw offers, 235 normalised, 235 ranked — proving the full pipeline (Gateway → Duffel → HttpxTransport → normalisation → FlightScorer → FlightReasoner → ranked results) against real sandbox data. Never part of `pytest`/CI. Zero changes to Trip Brain, the Explainability Engine, or any Discovery module besides Flights. Docs: `docs/LIVE_FLIGHT_SEARCH.md`, `docs/DUFFEL_SANDBOX_OPERATIONS.md`, `docs/ADR/ADR-024-live-flight-product-integration.md` (new); updated `docs/FIRST_LIVE_PROVIDER.md`, `docs/FLIGHT_PROVIDER_INTEGRATION.md`, `docs/PRODUCTION_READINESS.md`, `docs/ARCHITECTURE.md`, `README.md`, `.env.example`. 73 new automated tests (1032 total, all passing), every one against `FakeTransport`/`httpx.MockTransport` — config mode selection, per-capability gateway environment resolution (including proof Accommodation/Weather are unaffected), startup registration (including the fail-fast missing-token case and idempotency), fallback/failure behaviour (including "never mixed" and "never raises in MOCK mode"), request validation (every rule, plus MOCK-mode exemption), live response normalisation and partial-failure tolerance, provider-offer-id/data-source field handling, and no-secret-leakage assertions directly on live-mode HTTP responses. Ruff clean; frontend typecheck and production build both clean. Not committed — pending review, per explicit instruction |
| T-039 | Live Accommodation Search with Duffel Stays | `complete` | high | Connects Accommodation Intelligence to real Duffel Stays sandbox data, mirroring T-038's pattern for accommodation's real differences — explicitly did not assume the flight adapter's structure applies, and it didn't in two ways: destination resolution (Stays needs coordinates, not a place name) and an explicit Normaliser stage (Flights maps a vendor offer almost directly). **Access finding**: the existing `DUFFEL_API_TOKEN` (used for Flights since T-027) does **not** have Duffel Stays access — confirmed via a direct live check, a clean HTTP 403, `"This feature is not enabled for your account. Please contact sales to get access."` Per this task's own instructions, the adapter was built and fully unit-tested against documented Duffel-shaped fixtures regardless, and the manual verification script was still run to completion, producing this exact 403 as its correct, documented outcome (see Deliverable 5 below). **Configuration**: `ConfigurationManager` gained `accommodation_provider_mode`/`accommodation_mock_fallback_enabled`, independent of `flight_provider_mode` and of the general `provider_environment`. **Gateway**: `IntelligenceGateway._environment_for(capability)` (T-038) generalised from a single FLIGHTS-only branch into a `Capability -> config attribute` map (`_CAPABILITY_MODE_CONFIG_ATTR`) — adding ACCOMMODATION cost one dict entry, not a second gateway instance; Weather untouched. **Adapter**: new `travelos/live_providers/adapters/duffel_stays_provider.py` — `DuffelStaysProvider(BaseLiveProvider)`, `POST /stays/search` (`Duffel-Version: v2`, same bearer token as Flights). `_resolve_destination()` calls Duffel's own Places API (`GET /places/suggestions`) as a private, self-contained second transport call inside `build_request()` — no Duffel location data ever reaches the Discovery layer. **Real bug found and fixed via live testing** (the direct analogue of T-037's day-component-duration finding): querying "Tokyo" returns a `city`-type place with `latitude`/`longitude` both `null`, while `airport`-type places for the same query carry coordinates — the adapter's original "take the first city match" assumption failed against this exact real response; fixed to prefer the first *geocoded* place regardless of type, confirmed by a regression test built from the real response shape. `parse_response()` produces *raw, Duffel-vocabulary* dicts (`_provider_source: "duffel_stays"`), not already-canonical fields — `AccommodationNormalizer.normalize()` (`ai/discovery/accommodation/accommodation_normalizer.py`) gained a `_normalize_duffel_stays()` branch to absorb them, exactly the role its own docstring already described, leaving the mock-provider path byte-identical. Every field Duffel doesn't provide gets a documented, neutral default, never a fabricated value: `safety_score` always `0.5` (no Duffel signal exists at all), `distance_to_transport` set equal to the *computed* `distance_to_centre` (itself derived via the haversine formula between the resolved search coordinates and the property's — real geometry, not invented), `accommodation_type` always `HOTEL` (Duffel's schema has no property-type field), `comfort_score` substitutes `review_score` for the mock's unavailable `cleanliness_rating`. Partial mapping failure tolerated the same way T-038 added it for flights (one bad search result skipped and counted, not a whole-response failure). **Request extension**: `adults`/`children`/`rooms` added to `MockAccommodationProvider.search()` (accepted, ignored — mock inventory still doesn't vary by occupancy) and `GatewayAccommodationProvider.search()` (forwarded to Duffel, which requires them); `AccommodationIntelligence.recommend()` and `RecommendAccommodationRequest` gained a `rooms` parameter/field. **Validation**: new `ai/discovery/accommodation/live_search_validator.py` (destination required, check-in not in the past, 1–99 nights, 1–9 adults, 1–8 rooms) — LIVE_SANDBOX-only, exempting MOCK mode exactly as T-038 did for flights. **Public API**: `AccommodationRecommendationResponse` gained `data_source`/`provider_status`/`retrieved_at`/`request_id`/`raw_results_count`/`normalised_results_count`/`ranked_results_count`; each `AccommodationOptionResponse` also gained `data_source`. `AccommodationOption.provider_property_id`/`provider_rate_id` preserve Duffel's own identifiers internally for future booking work, excluded from `to_dict()`'s public shape. **Failure/fallback**: new `LiveAccommodationSearchUnavailableError` — `503` by default, `TRALVANA_ACCOMMODATION_MOCK_FALLBACK_ENABLED=true` to fall back to labelled mock data, mock and live properties never blended. **Startup wiring**: new `travelos/live_providers/accommodation_provider_bootstrap.py`'s `configure_accommodation_provider()`, called from `services/api/app/main.py` alongside T-038's flight equivalent — a no-op in MOCK mode; fails startup if LIVE_SANDBOX is set without a token (Stays-enablement failures happen per-request, not at startup, since a valid Flights token doesn't guarantee Stays access). **Frontend**: `apps/web/src/app/accommodation/recommend/page.tsx` gained a Rooms field, a source badge, a sandbox/fallback banner ("Duffel Stays sandbox data — not available for purchase."), and breakfast/cancellation/stay-total display on every card; `apps/web/src/app/accommodation/[id]/page.tsx` gained the same banner driven by the per-option `data_source`. No booking button anywhere. **Verification**: new `scripts/verify_duffel_stays_live_sandbox.py` — run live during this task: Places lookup succeeded (200), search call correctly returned the documented 403, reported cleanly with no token or payload exposure. Never part of `pytest`/CI. Zero changes to Trip Brain, the Explainability Engine, `AccommodationScorer`'s weights, or any Discovery module besides Accommodation. Docs: `docs/LIVE_ACCOMMODATION_SEARCH.md`, `docs/DUFFEL_STAYS_INTEGRATION.md`, `docs/ADR/ADR-025-duffel-stays-integration.md` (new); updated `docs/PRODUCTION_READINESS.md`, `docs/INTELLIGENCE_GATEWAY.md`, `docs/ARCHITECTURE.md`, `README.md`, `.env.example`. 129 new automated tests (1161 total, all passing), every one against `FakeTransport`/`httpx.MockTransport` — provider registration, mode selection (including cross-capability independence from `flight_provider_mode`), missing-credential fail-fast, destination resolution (including the city-without-coordinates regression), request/response mapping, price/currency/cancellation/breakfast mapping, missing-optional-field defaults, zero-results, partial mapping failure, every standard error type, the account-not-enabled 403 specifically, metadata redaction, no-secret-leakage, explicit fallback, and public API compatibility. Ruff clean; frontend typecheck and production build both clean. Not committed — pending review, per explicit instruction |
| T-040 | AI Travel Planner (Trip Assembly Engine) | `complete` | high | Makes natural-language trip planning the primary user experience — until now, Trip Brain's real six-module orchestration was only reachable via the backend-only `POST /conversation/message`, while the only frontend trip-planning page (`/trips/new`) called Sprint-1 legacy logic (`ai/planning/trip_planner.py`) that never touched Trip Brain or the real Discovery modules at all. **Architecture**: `ai/trip_brain/trip_assembly.py`'s new `TripAssemblyEngine` is a second, separate caller of Trip Brain's output — `ai/trip_brain/coordinator.py` (`TripBrain.plan()`) is byte-for-byte unchanged, its 75+ existing tests untouched; the same relationship `ai/concierge/conversation_engine.py` and `POST /explain` already have with Trip Brain. Every one of the itinerary's 13 required sections reads a field a Discovery module, Trip Brain, or the Explainability Engine already computed — `top_option` for destination/flight/accommodation/budget, the whole assessment dict for visa/weather, `explanation["risks"/"assumptions"/"recommendation_drivers"/"confidence_explanation"/"alternatives_considered"]` verbatim, `overall_confidence` as-is. Zero new scoring, zero recalculation. **Daily outline**: the one genuinely new *capability* — reuses `ai/planning/itinerary_builder.py` (T-008, Sprint 1's deterministic goal-type-templated day planner, complete with real KG landmark/restaurant enrichment), called for the first time from a Trip-Brain-backed path rather than duplicated. **Executive summary**: a new natural-language template assembled from facts each module already decided (destination, flight airline+price, property name, budget tier, visa outcome, weather fit) — every clause conditional on the fact it quotes actually being present, never backfilling a module that didn't run. **API**: new `POST /planner/plan` (`services/api/app/routers/planner.py`) — a thin wrapper reusing `travel_concierge.handle()`/`ConversationEngine.process()` wholesale (intent classification, goal/trip auto-creation, session management, all unchanged), adding only the Trip Assembly step when `session.last_recommendation` was populated; `itinerary: null` with a conversational follow-up otherwise, exactly matching `POST /conversation/message`'s existing behaviour for insufficient information. **Frontend**: new `apps/web/src/app/planner/page.tsx` — one natural-language text box (no per-module forms), rendering all 13 sections including a day-by-day grid; homepage (`apps/web/src/app/page.tsx`) now leads with "Plan My Trip" as the primary call to action. **Verified live in a real browser during this task** (not just curl/pytest): "I want to plan a football trip to London in September for 2 adults, balanced budget, travelling from Nigeria" produced a complete itinerary — all six Discovery modules ran, the executive summary correctly quoted real flight/accommodation/visa/weather facts, and the daily outline correctly used football-themed days with real London landmark enrichment (Buckingham Palace, Tower of London, Borough Market). Legacy `/trips/plan`/`ai/planning/trip_planner.py` left untouched, running in parallel, per "preserve the existing architecture." Docs: `docs/AI_TRAVEL_PLANNER.md`, `docs/ADR/ADR-026-trip-assembly-engine.md` (new); updated `docs/ARCHITECTURE.md`. 29 new automated tests (1190 total, all passing) — `ai/tests/test_trip_assembly.py` (23 tests, every assertion checks a value was *read* from a pre-built `AgentResult`/`explanation`, never that it was computed, including graceful handling of failed/missing modules and proof the executive summary never fabricates a fact from a module that didn't run) and `services/api/tests/test_planner.py` (6 tests — full natural-language message end-to-end, vague message with no itinerary, no internal-field leakage, multi-turn conversation continuity). Ruff clean; frontend typecheck and production build both clean. No booking, payment, or reservation logic anywhere. Not committed — pending review, per explicit instruction |
| T-041 | Launch Stabilisation | `complete` | critical | Corrects local and Docker API import paths; standardises web/API ports at 3001/8000; adds Windows PowerShell launchers; moves CORS into environment-backed configuration; upgrades and pins Next.js 15.5.20 plus matching ESLint config; adds `package-lock.json` and reproducible `npm ci`; makes lint/build CI jobs blocking. Validation: 1,191 tests, Ruff, frontend lint/build, API health/planner, frontend port 3001, and CORS preflight all pass. Docker Compose structure validated; engine build deferred to a Docker-equipped machine. ADR-027 |
| T-042 | Commercial Data Foundation | `complete` | critical | PostgreSQL-backed partner, affiliate-programme, outbound-click, conversion, and commission ledger; SQLAlchemy repository adapter; Alembic upgrade/downgrade migration; Compose PostgreSQL health gate and automatic migration; read-only safe diagnostics; privacy and financial constraints. No public booking, redirect, payment, webhook, or admin mutation behaviour. T-034 still owns Goal/Trip persistence. Validation: 1,200 tests, Ruff, Alembic schema-drift check, and frontend lint/build pass. ADR-028 |

---

## Backlog — Sprint 2 (current)

| Task ID | Title | Status | Priority | Notes |
|---------|-------|--------|----------|-------|
| T-012A | Platform Layer Test Coverage | `planned` | medium | Unit tests for `travelos/`: SDK, Service Registry, Event Bus, Configuration Manager, Service Container, shared base classes; see TD-015. Now unblocked — T-014 is complete |
| T-033 | Budget Optimisation Engine | `planned` | medium | Cross-trip budget optimisation, replaces static budget estimator paths; builds on T-018 |

---

## Backlog — Sprint 3

| Task ID | Title | Status | Priority | Notes |
|---------|-------|--------|----------|-------|
| T-034 | PostgreSQL persistence — Goals + Trips | `planned` | critical | Replace in-memory repositories; Alembic migrations |
| T-035 | Redis session store | `planned` | high | Replace in-memory conversation store; enables horizontal scaling |
| T-036 | Fix AI↔API dependency direction | `planned` | medium | Introduce PlanningPort interface; see REC-008, TD-006 |
| T-028 | Replace static KG enrichments with live queries | `planned` | medium | ItineraryBuilder reads from KnowledgeService at runtime; see REC-009, TD-010 |
| T-029 | Kuzu graph database | `planned` | medium | Replace in-memory KG with persistent Kuzu DB |
| T-030 | Promote Traveller to domain | `planned` | medium | Move to `services/api/app/domains/traveller/`; see REC-006, TD-011 |
| T-031 | Auth layer | `planned` | high | Clerk or NextAuth.js — protect API routes |
| T-032 | Migrate remaining intents off legacy TravelManager | `planned` | medium | Give `MODIFY_TRIP`, `DESTINATION_QUESTION`, `TRAVEL_ADVICE`, `BUDGET_ADVICE` a real Discovery-module or Trip Brain equivalent, replacing the Sprint-1 placeholder agents (`flight_agent`, `hotel_agent`, `experience_agent`, `budget_agent`) they still dispatch through. Prerequisite for actually deleting `ai/manager/`, `ai/registry/`, and the five placeholder agents — see TD-018, ADR-018 |

---

## Notes

- Task IDs are sequential and never reused.
- PR title format: `[T-013] GitHub Actions CI/CD`
- Blocked tasks include the blocker in Notes.
- Technical debt items tracked in `docs/TECHNICAL_DEBT_REGISTER.md`.
- Architecture recommendations in `docs/ARCHITECTURE_RECOMMENDATIONS.md`.
- 2026-07-08: Renumbered Sprint 2/3 backlog to match the actual roadmap in use (T-013 CI/CD → T-020 Budget Optimisation Engine). Previous T-011–T-023 numbering (legacy conversation removal, dead orchestration removal, etc.) is superseded — see `TECHNICAL_DEBT_REGISTER.md` for those items, now tracked as TD-001–TD-005 under T-014.
- 2026-07-08: T-014 closed TD-001, TD-002, TD-003, TD-004, TD-005, TD-016, TD-017. Investigation found TD-002/TD-004's original T-010 description had the two `AgentRegistry` implementations backwards — `ai/registry/agent_registry.py` (budget/experience/flight/hotel/visa agents) was the active one, not `ai/orchestration/agent_registry.py`. See TD-002/TD-004 correction notes in `TECHNICAL_DEBT_REGISTER.md` and ADR-009.
- 2026-07-10: Inserted Budget Intelligence Engine as T-018 (fourth Discovery Layer module, alongside Flight/Accommodation/Destination) and renumbered the rest of the backlog down: Visa Intelligence T-018→T-019, Weather & Safety T-019→T-020, Budget Optimisation Engine T-020→T-021, and Sprint 3 items T-021–T-027→T-022–T-028. No prior task carried these new numbers, so this is a pure insertion, not a reuse of closed IDs.
- 2026-07-10: T-019 Visa Intelligence Engine completed as the fifth Discovery Layer module.
- 2026-07-10: T-020 Weather & Safety Intelligence Engine completed as the sixth and final Discovery Layer module. All six planned Discovery modules (Flight, Accommodation, Destination, Budget, Visa, Weather & Safety) are now complete.
- 2026-07-10: Inserted Trip Brain Architecture as T-021 and renumbered the rest of the backlog down: Budget Optimisation Engine T-021→T-022, and Sprint 3 items T-022–T-028→T-023–T-029. Same pure-insertion pattern as the 2026-07-10 Budget Intelligence renumbering above — no prior task carried these new numbers.
- 2026-07-10: T-021 architecture approved with one change: legacy retirement (`ai/manager`, `ai/registry`, placeholder agents) is split out of the Trip Brain implementation task into its own later task, so the old path survives as a rollback option until Trip Brain is verified. Inserted T-022 (Trip Brain Implementation — build only, no legacy removal) and T-023 (Legacy Orchestration Cleanup — blocked until T-022 is verified), renumbering Budget Optimisation Engine T-022→T-024 and Sprint 3 items T-023–T-029→T-025–T-031. `docs/ADR/ADR-017-trip-brain.md`, `docs/TRIP_BRAIN_ARCHITECTURE.md`, and `docs/ORCHESTRATION_PATTERN.md` updated to reflect the split.
- 2026-07-11: T-022 Trip Brain Implementation completed. New `ai/trip_brain/` package built exactly to ADR-017/`docs/TRIP_BRAIN_ARCHITECTURE.md`; `Intent.PLAN_TRIP` now routes through it. `ai/manager/`, `ai/registry/`, and the five placeholder agents remain untouched, uncalled for `PLAN_TRIP`, as T-023's rollback path — all other broad-ish intents (`MODIFY_TRIP`, `DESTINATION_QUESTION`, `TRAVEL_ADVICE`, `BUDGET_ADVICE`) still dispatch through `TravelManager` unchanged. T-023 is now unblocked but not started. See the Completed Tasks table entry for full implementation detail, including a pre-existing `IntentClassifier` entity-extraction bug found and fixed along the way.
- 2026-07-11: T-023 Legacy Orchestration Retirement completed as an investigation, not a deletion — the prior note above and ADR-017 both overstated how dormant `ai/manager/`/`ai/registry/`/the five agents were. A full import trace found them still live for `MODIFY_TRIP`/`DESTINATION_QUESTION`/`TRAVEL_ADVICE`/`BUDGET_ADVICE`, so none of that code was deleted (deleting it would have broken those four intents). Corrected the affected documentation, added ADR-018, opened TD-018, and inserted T-032 (migrate those four intents off `TravelManager`) as the actual prerequisite for eventual deletion. No prior task numbers reused; T-032 is a pure append to the Sprint 3 backlog.
- 2026-07-11: Inserted Explainability Engine as T-024, displacing the planned Budget Optimisation Engine backlog item — renumbered it to T-033 (next unused id after T-032), left otherwise unchanged in the Sprint 2 backlog table. No prior task carried T-024's old meaning as completed work, so this is a pure reassignment of a still-`planned` id, not a reuse of closed work. T-024 Explainability Engine completed the same day — see the Completed Tasks table entry.
- 2026-07-11: Inserted Intelligence Gateway as T-025, displacing the planned "PostgreSQL persistence — Goals + Trips" Sprint 3 backlog item — renumbered it to T-034 (next unused id after T-033), left otherwise unchanged. Same pure-reassignment pattern as the Explainability Engine insertion above — no completed work's id was reused. T-025 Intelligence Gateway completed the same day — see the Completed Tasks table entry, including the `travelos/intelligence_gateway/` vs. the task's suggested `platform/intelligence_gateway/` path deviation (ADR-020).
- 2026-07-12: Inserted Live Provider Framework as T-026, displacing the planned "Redis session store" Sprint 3 backlog item — renumbered it to T-035 (next unused id after T-034), left otherwise unchanged. Same pure-reassignment pattern as the two insertions above — no completed work's id was reused. T-026 Live Provider Framework completed the same day — see the Completed Tasks table entry, including the finding that the Intelligence Gateway (T-025) needed zero code changes to support it (ADR-021).
- 2026-07-12: Assigned First Live Flight Provider Integration to T-027 at the requester's explicit instruction, superseding this id's prior meaning ("Fix AI↔API dependency direction," still `planned`, never started) — renumbered that item to T-036 (next unused id after T-035), left otherwise unchanged. Unlike every prior renumbering in this log, this one reassigns an id that already had a defined-but-unstarted meaning rather than inserting into an unused one; no completed work's id was touched, and the displaced task's content, priority, and status are unchanged, only its id moved. T-027 First Live Flight Provider Integration completed the same day — see the Completed Tasks table entry, including the Duffel-over-Amadeus evaluation (`docs/FIRST_LIVE_PROVIDER.md`, ADR-022).
- 2026-07-12: Created T-037 (Real HTTP Transport and First Live Provider Integration) at the requester's explicit instruction, using the next unused id (T-036 having just been claimed by the renumbered AI↔API dependency task) rather than reassigning an in-use one — a pure append, same pattern as T-032/T-033's original insertion. Completed the same day: `HttpxTransport` built and unit-tested, then verified with one real Duffel SANDBOX call using a user-supplied token (never read/printed/logged/committed by this work) — found and fixed a real ISO 8601 duration parsing bug (`P1DT5H15M`, a day component `FakeTransport`-only testing had no way to surface) along the way. See the Completed Tasks table entry and `docs/ADR/ADR-023-real-http-transport-and-live-verification.md`. Not committed — awaiting review.
